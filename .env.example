# GitHub Remote MCP Server Configuration
# GitHub MCP サーバーのエンドポイント URL（GitHub 公式の Remote MCP Server）
GITHUB_MCP_URL=https://api.githubcopilot.com/mcp/

# GitHub Personal Access Token (PAT)
# スコープ: repo, read:org (最小限必要なスコープのみ)
# トークンの作成: https://github.com/settings/tokens
GITHUB_MCP_TOKEN=ghp_your_personal_access_token_here

# Sentry MCP Server Configuration (STDIO モード)
# 注意: Remote MCP Server (https://mcp.sentry.dev/mcp) は OAuth 専用のため、
# ローカル STDIO モードで接続します

# Sentry User Auth Token (必須)
# スコープ: org:read, project:read, project:write, team:read, team:write, event:write
# トークンの作成: https://mov-inc.sentry.io/settings/account/api/auth-tokens/
SENTRY_MCP_TOKEN=sntrys_your_auth_token_here

# Sentry URL (必須)
# あなたの組織の完全な URL を指定
SENTRY_MCP_URL=https://mov-inc.sentry.io

# Notion MCP Server Configuration (STDIO モード)
# Notion のインテグレーションキー（Internal Integration Secret）を使用して接続します

# Notion Internal Integration Secret (必須)
# 形式: ntn_********
# トークンの作成手順:
# 1. https://www.notion.so/my-integrations にアクセス
# 2. 新規統合（New integration）を作成
# 3. Configuration タブで Internal Integration Secret をコピー
# 4. 対象ページで「...」→「Connect to integration」で統合を接続
# 注意: ページごとに統合の接続が必要です（接続しないとアクセスできません）
NOTION_TOKEN=ntn_your_integration_secret_here
